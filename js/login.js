console.log("Department Login Page Loaded");

const form = document.getElementById("loginForm");

form.addEventListener("submit", function(e){

    e.preventDefault();

    alert("Login system backend will be connected later using Django.");

});